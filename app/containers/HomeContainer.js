var React = require('react');
var Home = require('../components/home');

var Home = React.createClass({
  render: function() {
    return (
      <div>Home </div>
    )
  }
});

module.exports = Home;
